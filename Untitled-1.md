https://windsurf.com/codemaps/f4287433-02fe-4abe-a569-69317ddb16aa-57492e78faf49728

1
معالجة تسرّب بيانات MongoDB في backend/.env.example (إزالة connection string الحقيقي ووضع placeholder)

تدوير حساب MongoDB الفعلي في لوحة MongoDB

مراجعة جميع ملفات .env.example والتأكد من عدم وجود مفاتيح حقيقية

إصلاح ملف env.ts في Frontend (استخدام z.infer، تعريف ServerEnv وClientEnv، إزالة casts المكسورة)

إنشاء/استكمال 18 ملف مفقود في Frontend

إصلاح أخطاء صرامة الأنواع (~40 خطأ) في creative-development.tsx، useProject.ts، landing-card-scanner.tsx

تشغيل pnpm typecheck في Frontend والتحقق من عدم وجود أخطاء

إزالة ignoreBuildErrors وignoredDuringBuilds من next.config.ts

تشغيل pnpm build في Frontend والتحقق من نجاح البناء

استبدال stubs في api.ts بتكامل فعلي مع Backend

استبدال stubs في gemini-core.ts بوظائف تستدعي Backend فعلياً

تطبيق فهارس قاعدة البيانات الجديدة (تشغيل pnpm db:generate ثم pnpm db:push)

إعادة كتابة منطق Controllers لاستخدام استعلام JOIN واحد

تفعيل التخزين المؤقت Redis (دمج analyzeWithCache من gemini-cache.strategy.ts)

استبدال وسوم <img> بـ next/image في Frontend

تطبيق التحميل الكسول (Lazy Loading) للمكونات الثقيلة مثل Particles

إتمام توحيد استيرادات Frontend (تحويل 44 استيراد نسبي متبقي إلى aliases)

إزالة Dependencies المهملة

تنفيذ توصيات تقرير DELETION_CANDIDATES_REPORT.md